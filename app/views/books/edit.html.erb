<% provide(:title, @book.title) %>
<%= render 'book_header' %>

<h1>Editing book</h1>

<%= render 'form' %>

<%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>

<%= render 'layouts/footnote' %>
