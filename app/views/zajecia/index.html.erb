<h1>Listing zajecia</h1>

<table>
  <tr>
    <th>Temat</th>
    <th>Opis</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @zajecia.each do |zajecium| %>
  <tr>
    <td><%= zajecium.temat %></td>
    <td><%= zajecium.opis %></td>
    <td><%= link_to 'Show', zajecium %></td>
    <td><%= link_to 'Edit', edit_zajecium_path(zajecium) %></td>
    <td><%= link_to 'Destroy', zajecium, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Zajecium', new_zajecium_path %>
